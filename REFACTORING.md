# Оптимизация app.js - Архитектура сервисов

## Что было сделано

`app.js` был "переполнен" логикой разных слоев приложения. Я разделил функционал на специализированные модули-сервисы в папке `js/services/`.

## Новая структура

### 📁 js/services/

#### 1. **authService.js** - Аутентификация
- `login(password)` - попытка входа
- `getSavedPassword()` - получить пароль из localStorage
- `savePassword(password)` - сохранить пароль
- `clearPassword()` - удалить пароль
- `restoreSession()` - автовход при загрузке

#### 2. **dataService.js** - Работа с данными
- `loadFiles()` - загрузить галерею
- `loadConfig()` - загрузить конфиг товаров
- `saveConfig(password, products)` - сохранить конфиг
- `uploadFiles(password, fileList)` - загрузить файлы
- `uploadToCloud(password, blob, fileName, article, category)` - загрузить в облако
- `mergeProducts(savedConfig)` - объединить товары с конфигом
- `initialize()` - полная инициализация данных

#### 3. **pngService.js** - Работа с PNG
- `setPNGDPI(blob, dpi)` - установить DPI метаданные в PNG

#### 4. **renderService.js** - Рендеринг мокапов
- `buildDefaultTransforms(products, mode)` - создать дефолтные трансформации
- `getDefaultScale(mode)` - дефолтный масштаб
- `initializeTransforms(file, products, mode)` - инициализировать трансформации при выборе принта
- `getTransformByMode(transforms, productTransforms, mode, productId, fallbackScale)` - получить трансформацию
- `renderMockupBlob(product, printImg, transform, mockupDPI, mockupWidth, mockupHeight, options)` - отрендерить мокап

#### 5. **exportService.js** - Экспорт
- `exportToZip(selectedPrint, products, transforms, productTransforms, activeTab, mockupDPI, mockupWidth, mockupHeight)` - экспортировать в ZIP
- `saveToCloud(selectedPrint, products, transforms, productTransforms, password, activeTab, cloudMode, mockupDPI, mockupWidth, mockupHeight, onProgress)` - сохранить в облако

## Преимущества

✅ **app.js сократился с 662 до ~350 строк** (уменьшение на 47%)
✅ **Модульная архитектура** - каждый сервис отвечает за свою область
✅ **Легче тестировать** - отдельные функции можно тестировать независимо
✅ **Легче поддерживать** - изменение логики в одном месте
✅ **Переиспользуемость** - сервисы можно использовать в других компонентах
✅ **Чистый код** - app.js теперь фокусируется на управлении состоянием и UI

## Порядок подключения в index.html

Важно соблюдать порядок:
1. `constants.js` - глобальные константы
2. `utils.js` - утилиты
3. `services/*.js` - сервисы (могут зависеть от utils)
4. `components/*.js` - компоненты (могут использовать сервисы)
5. `app.js` - главный компонент (использует всё выше)

## Примеры использования

```javascript
// Аутентификация
const password = await window.AuthService.restoreSession();

// Загрузка данных
const { files, products } = await window.DataService.initialize();

// Экспорт
const zipBlob = await window.ExportService.exportToZip(...);
```
