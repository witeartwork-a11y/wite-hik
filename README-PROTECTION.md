# ⚡ Быстрый старт - Защита данных

## Что было сделано:

✅ Создан `.gitignore` - защищает от перезаписи:
   - `uploads/` - все загруженные файлы и облако
   - `data/products_config.json` - ваша конфигурация товаров
   - `data/thumbnails/` - кэш превью
   - `.env.local` - локальные пароли и настройки

✅ Создан `.env.example` - пример конфигурации

✅ Созданы `.gitkeep` файлы - сохраняют структуру пустых папок

✅ Созданы документации:
   - `SETUP.md` - полная инструкция настройки на сервере
   - `PROTECTION.md` - защита при git pull

✅ Создан `setup-protection.sh` - автоматизированный скрипт

---

## 🚀 Что делать дальше:

### Вариант 1: Автоматически (рекомендуется)
```bash
bash setup-protection.sh
```

### Вариант 2: Вручную
```bash
# 1. Удалить файлы из отслеживания git (если они там были)
git rm --cached data/products_config.json 2>/dev/null
git rm --cached -r uploads/ 2>/dev/null
git rm --cached -r data/thumbnails/ 2>/dev/null

# 2. Обновить индекс git
git add .gitignore .env.example PROTECTION.md SETUP.md setup-protection.sh

# 3. Сделать коммит
git commit -m "🔒 Add data protection, gitignore, and setup scripts"

# 4. Загрузить на GitHub
git push origin main
```

---

## 🎯 На сервере при git pull:

```bash
# После первой загрузки защиты на GitHub:
cd /var/www/html/wite-hik
git pull origin main

# Ваши данные не потеряются! Защищены:
# ✅ uploads/
# ✅ data/products_config.json
# ✅ data/thumbnails/
```

---

## 📋 Проверка:

```bash
# Посмотрите что будет проигнорировано
git status
# Не должны быть там uploads/, data/products_config.json и т.д.

# Проверьте содержимое .gitignore
cat .gitignore
```

---

## ⚠️ Важное:

1. **Измените пароль** в `api.php`:
   - Найдите: `$PASSWORD = 'hikomori1hikomori1';`
   - Замените на свой пароль

2. **После первого pull на сервере** запустите:
   ```bash
   bash setup-protection.sh
   ```

3. **Никогда не коммитьте**:
   - `.env.local` (реальные пароли)
   - `uploads/` (пользовательские файлы)
   - `data/products_config.json` (ваша конфигурация)

---

Готово! 🎨 Ваши данные теперь защищены при любых git pull! 🔒
