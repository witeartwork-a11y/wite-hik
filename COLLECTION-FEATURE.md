# 📦 Новая функциональность: Коллекция принтов для облака

## Описание

Была добавлена новая система для работы с коллекцией принтов перед сохранением в облако:

### ✨ Основные возможности:

1. **Левая колонка "Принты на облако"**
   - Добавление принтов в коллекцию со специальной кнопкой
   - Отображение всех добавленных принтов с их данными
   - Максимум 10 принтов видно в списке (со скроллингом)

2. **Редактирование артикула (имя для сохранения)**
   - Каждый принт в коллекции имеет артикул (по умолчанию = имя файла)
   - Кликните на артикул или нажмите иконку карандаша для редактирования
   - Артикул используется как имя папки в облаке

3. **Сохранение позиций на канве**
   - Когда вы добавляете принт в коллекцию, сохраняются его текущие позиции на всех товарах
   - Позиции включают: X, Y координаты, масштаб, угол поворота
   - При сохранении в облако используются именно эти позиции

4. **Множественный выбор и сохранение**
   - Выберите 1 или несколько принтов в коллекции (чекбоксы)
   - Нажмите "Добавить N принтов в облако"
   - Процесс запустится и сохранит все выбранные принты
   - Каждый принт сохраняется в свою папку с названием его артикула

5. **Удаление из коллекции**
   - Нажмите иконку корзины рядом с принтом для удаления
   - Также при удалении файла из галереи удаляются все его экземпляры из коллекции

---

## 🎯 Сценарий использования:

### Шаг 1: Подготовка
```
1. Откройте вкладку "Мокапы" или "Товары"
2. Настройте позиции изображения на канве для каждого товара
```

### Шаг 2: Добавление в коллекцию
```
1. Наведите мышь на изображение в галерее
2. Нажмите кнопку "+ " (плюс в круге) для добавления
   ИЛИ
   Откройте вкладку "Файлы" в базе
   Наведите на изображение
   Нажмите "Add to Cloud"
```

### Шаг 3: Редактирование артикула (опционально)
```
1. В левой колонке найдите добавленный принт
2. Кликните на текущий артикул (моно-шрифт)
3. Введите новое название (например: "DESIGN-001")
4. Нажмите Enter или кликните вне поля
```

### Шаг 4: Выбор принтов
```
1. Отметьте чекбоксы рядом с нужными принтами
2. Количество выбранных принтов показано в значке-бейдже
```

### Шаг 5: Сохранение в облако
```
1. Убедитесь что выбрали нужные принты
2. Нажмите "Добавить N принтов в облако"
3. Процесс начнется (видна прогресс-панель)
4. После завершения принты удалятся из коллекции
```

---

## 📁 Структура сохранения в облако

Каждый принт сохраняется с такой структурой:

```
uploads/cloud/
├── article-001/           (название папки = артикул)
│   ├── mockups/
│   │   ├── KVR_article-001.png
│   │   ├── MUG_article-001.png
│   │   └── ...
│   └── products/
│       ├── KVR_article-001.png
│       └── ...
└── article-002/
    ├── mockups/
    └── products/
```

Где:
- `article-001`, `article-002` - имена из артикулов
- `KVR`, `MUG` и т.д. - префиксы товаров
- Файлы содержат готовые мокапы с позициями из коллекции

---

## 🔧 Технические детали

### Новые компоненты:
- **PrintCollection.js** - компонент левой колонки с управлением принтами

### Модифицированные компоненты:
- **app.js** - добавлены состояния и функции для коллекции
- **Gallery.js** - добавлена кнопка для добавления в коллекцию
- **index.html** - подключен новый компонент

### Новые состояния (App.js):
```javascript
const [printCollection, setPrintCollection] = useState([]);
const [selectedPrintIds, setSelectedPrintIds] = useState([]);
```

### Новые функции (App.js):
- `handleAddPrintToCollection(file)` - добавление в коллекцию
- `handleSelectPrintInCollection(printId)` - выбор/отмена
- `handleRemovePrintFromCollection(printId)` - удаление
- `handleUpdateArticle(printId, newArticle)` - сохранение артикула
- `handleDeleteFile(fileName)` - очистка при удалении файла
- `handleSaveCollectionToCloud(printIds)` - сохранение в облако

---

## ⚡ Быстрые советы:

1. **Сохраняйте позиции перед добавлением**: убедитесь что изображение хорошо позиционировано на всех товарах, прежде чем добавлять в коллекцию

2. **Используйте осмысленные артикулы**: артикул становится названием папки в облаке, используйте уникальные имена

3. **Множественное сохранение**: добавьте несколько принтов в коллекцию и сохраните их за раз - это быстрее чем по одному

4. **Прогресс виден**: в процессе сохранения видна прогресс-панель с информацией о текущем товаре

5. **Автоочистка**: после успешного сохранения принты автоматически удаляются из коллекции

---

## 🐛 Что делать при проблемах:

### Принт не добавляется
- Убедитесь что выбрали правильный товар для отображения
- Проверьте что изображение загружено

### Артикул не сохраняется
- Убедитесь что вышли из режима редактирования (нажали Enter)
- Попробуйте еще раз

### Ошибка при сохранении в облако
- Проверьте подключение к интернету
- Убедитесь что выбрали товары (они должны быть отмечены галочкой)
- Попробуйте сохранить по одному принту

---

## 📝 Заметки по разработке:

- Позиции сохраняются для всех включенных товаров одновременно
- При сохранении используются именно те позиции которые были при добавлении в коллекцию
- Если товар был отключен при добавлении, его позиции не будут сохранены
- После изменения товаров в основном списке рекомендуется пересоздать коллекцию

