# Защита данных при git pull

## 📋 Краткая инструкция

### 1. Файлы и папки, защищённые от перезаписи:
- ✅ `data/products_config.json` - конфигурация товаров
- ✅ `data/thumbnails/` - все превью изображений
- ✅ `uploads/` - все загруженные пользователями файлы
- ✅ `uploads/cloud/` - все файлы облака

### 2. При `git pull`:
```bash
git pull origin main
```
Все эти файлы **не будут перезаписаны**, ваши данные сохранятся.

### 3. Если нужно обновить конфигурацию:

#### Вариант 1: Используйте `.env` файл
```bash
cp .env.example .env.local
# Отредактируйте .env.local с вашими значениями
```

#### Вариант 2: Вручную объедините конфиги
```bash
# Сначала сделайте backup
cp data/products_config.json data/products_config.json.backup

# Затем проверьте что изменилось
git diff data/products_config.json

# Если нужны новые поля из обновления - вручную добавьте их
```

## 🔒 Структура защиты

### .gitignore защищает от случайной фиксации:
- Локальные конфиги (`.env`, `.env.local`)
- Пользовательские файлы (`uploads/`)
- Кэш и превью (`data/thumbnails/`)

### .gitkeep сохраняет структуру папок:
- Пустые папки всё равно отслеживаются
- При клонировании структура создаётся автоматически

## ⚠️ Важные замечания

1. **Первый раз после настройки:**
```bash
# Удалите отслеживание уже добавленных файлов
git rm --cached data/products_config.json
git rm --cached -r uploads/
git rm --cached -r data/thumbnails/

# Обновите repo
git commit -m "Protect local config and uploads from git tracking"
```

2. **Если конфиги уже в репозитории:**
Попросите мейнтейнера проекта переместить их в `.env.example`

3. **Безопасность пароля:**
- Никогда не коммитьте реальные пароли!
- Используйте только `.env.example` с примерами
- Реальные пароли только в `.env.local`

## 🚀 Рекомендуемая структура

```
wite-hik/
├── .gitignore          ✅ Защита от коммита
├── .env.example        ✅ Пример конфига
├── .env.local          ❌ НЕ коммитить (реальные данные)
├── api.php
├── data/
│   ├── products_config.json      ❌ НЕ коммитить
│   ├── thumbnails/
│   │   └── .gitkeep
│   └── files.json
├── uploads/
│   ├── .gitkeep
│   └── cloud/.gitkeep
└── ... остальное
```

## 🔄 Обновление после git pull

Если нужно проверить, что обновилось:

```bash
# Посмотрите логи
git log --oneline -5

# Проверьте изменившиеся файлы
git diff HEAD~1

# Если что-то сломалось, откатитесь
git revert HEAD
```
